pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Add your build steps here
                // For example, you can use the 'sh' step to execute shell commands
                sh 'echo "Building..."'
            }
        }
        
        stage('Test') {
            steps {
                // Add your test steps here
                // For example, you can use the 'sh' step to execute shell commands
                sh 'echo "Running tests..."'
            }
        }
        
        stage('Deploy') {
            steps {
                // Add your deployment steps here
                // For example, you can use the 'sh' step to execute shell commands
                sh 'echo "Deploying..."'
            }
        }
    }
}